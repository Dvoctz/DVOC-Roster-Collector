
import { Division, Position } from './types';

export const DIVISIONS: Division[] = Object.values(Division);
export const POSITIONS: Position[] = Object.values(Position);
export const MAX_PLAYERS = 50;
